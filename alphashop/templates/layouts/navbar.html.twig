<nav class="navbar navbar-expand-lg bg-body-tertiary"data-bs-theme='dark'>
  <div class="container">
    <a class="navbar-brand brand-alpha" href="{{ path('app_home_page') }}">
    <span class="alpha-symbol">α </span> lpha<span class="shop-text">Shop</span>
</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link active" aria-current="page" href="{{ path('app_home_page') }}">Home</a>
        </li>

        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="{{ path('app_book_index') }}" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            Catégories
          </a>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="{{ path('app_book_index') }}">Catalogue</a></li>
            <li><a class="dropdown-item" href="{{ path('app_emprunt_en_cours') }}">Mes emprunts</a></li>
            <li><hr class="dropdown-divider"></li>
            <li><a class="dropdown-item" href="">Gigachad Face Classroom</a></li>
          </ul>
        </li>
        {% if is_granted("ROLE_ADMIN") %}
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="{{ path('app_book_index') }}" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            ADMIN
          </a>
          <ul class="dropdown-menu">
            <li><a class="dropdown-item" href="{{ path('app_book_index') }}">Voir livres</a></li>
            <li><a class="dropdown-item" href="{{ path('app_emprunt_en_cours') }}">Liste des emprunts</a></li>
            <li><a class="dropdown-item" href="{{ path('app_book_new') }}">Ajouter un livre</a></li>
            <li><a class="dropdown-item" href="">Produits</a></li>
            <li><hr class="dropdown-divider"></li>
            <li><a class="dropdown-item" href="{{ path('app_user_index') }}">Utilisateurs</a></li>
          </ul>
        </li>
        {% endif %}

        {% if app.user %} {#-- SI connecté ALORS se déconnester AUTREMENT s'inscrire / se connecter --#}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_logout') }}">Déconnexion ({{ app.user.userIdentifier }})</a>
            </li>
        {% else %}
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_register') }}">S'inscrire</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('app_login') }}">Se connecter</a>
            </li>
        {% endif %}

      </ul>
      <form class="d-flex" role="search">
        <input class="form-control me-2" type="search" placeholder="Rechercher..." aria-label="Search"/>
        <button class="btn btn-outline-success" type="submit">Rechercher</button>
      </form>
    </div>
  </div>
</nav>